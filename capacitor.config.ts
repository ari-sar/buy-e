import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dot.seller.com',
  appName: 'Seller',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
